#!/bin/ash -xe
if [ "$(find /etc/php7-kube-conf.d -type f -iname \*.ini | wc -l)" -gt 0 ] ; then
    cp -v /etc/php7-kube-conf.d/*.ini /etc/php7/conf.d
fi
if [ "$(find /etc/php7-fpm-kube.d -type f -iname \*.conf | wc -l)" -gt 0 ] ; then
    cp -v /etc/php7-fpm-kube.d/*.conf /etc/php7/php-fpm.d/
fi

exec /usr/sbin/php-fpm7 $*