FROM quay.io/simbelmas/fedora-minimal:v44.2@sha256:1cef69e363d0259f73866a024958df4b4188ca32fcb65db978723b4e93ea2ed0

LABEL maintainer "Simon Belmas <slos@soket.fr>"

RUN microdnf --nodocs --best -y install socat \
    && microdnf clean all

RUN echo 'app:x:1001:' >> /etc/group \
    && echo 'app:x:1001:1001::/app:/sbin/nologin' >> /etc/passwd \
    && mkdir -p /app /app/creds /app/config \
    && chown -Rv app:app /app \
    && chmod -R 755 /app

USER 1001

ENTRYPOINT ["socat"]
CMD ["-h"]
